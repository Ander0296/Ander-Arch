# Paquetes AUR (yay) — uno por línea. NVIDIA legacy va en README fase 4.2.
# Regla: instalaste algo nuevo con yay → agregalo acá y commiteá.

brave-bin
1password
ttf-jetbrains-mono-nerd
onlyoffice-bin
notion-app-electron

# --- WhatsApp (cliente nativo, prebuilt) ---
zapzap-bin

# --- IDE Java (AUR) ---
eclipse-java-bin

# --- Wallpaper Engine (reimplementación nativa para Linux) ---
# Solo el motor de render, SIN interfaz: wallpaperengine-gui se quitó porque
# consumía demasiada GPU y arrancaba solo al inicio. Se deja el motor instalado
# porque es el backend que usa skwd-wall (el selector que vamos a evaluar
# cuando salga su reescritura en Rust). Sin GUI no arranca nada por su cuenta.
# Requiere Steam (pacman, multilib) + poseer Wallpaper Engine + wallpapers
# suscriptos en la Workshop para que haya algo que reproducir.
linux-wallpaperengine-bin
