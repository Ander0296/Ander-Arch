# Paquetes AUR (yay) — uno por línea. NVIDIA legacy va en README fase 4.2.
# Regla: instalaste algo nuevo con yay → agregalo acá y commiteá.

brave-bin
1password
ttf-jetbrains-mono-nerd
onlyoffice-bin
notion-app-electron

# --- WhatsApp (cliente nativo, prebuilt) ---
zapzap-bin

# --- IDE Java (AUR) ---
eclipse-java-bin
intellij-idea-ultimate-edition

# --- Wallpaper Engine — QUITADO ---
# linux-wallpaperengine-bin se borró del AUR (la RPC no lo encuentra más) y
# rompía el bootstrap en cada PC nuevo. Sigue instalado en las máquinas donde
# ya estaba. Si hace falta reponerlo, el sucesor es linux-wallpaperengine-git,
# mantenido por Almamu (autor upstream); OJO: linux-wallpaper-engine-bin, con
# guión, es OTRO proyecto distinto. Decisión a revisar cuando salga la
# reescritura en Rust de skwd-wall, que lo usa de backend.

# --- Cliente de escritorio de YouTube Music (Tauri, prebuilt del .deb oficial) ---
# Se elige `zuno` (mantenido por el autor upstream) sobre `zuno-bin` porque
# declara gst-libav: sin ese plugin no hay decodificación AAC/H.264 y la
# reproducción muere con "GStreamer element appsink not found".
zuno
