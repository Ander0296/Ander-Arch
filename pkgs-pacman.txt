# Paquetes oficiales (pacman) — uno por línea. Los de GPU van en README fase 4.
# Regla: instalaste algo nuevo con pacman → agregalo acá y commiteá.

# --- CLI base / LazyVim + git ---
neovim
git
ripgrep
fd
fzf
tree-sitter-cli
gcc
lazygit
git-delta
github-cli
wl-clipboard
nodejs
npm

# --- CLI extras ---
bat
sd
tealdeer
btop
ttyper

# --- Conversión de documentos (función md2pdf en fish/functions) ---
# pandoc traduce entre formatos, pero para emitir PDF necesita un motor de
# tipografía. El clásico es LaTeX (texlive-full = varios GB); typst hace lo
# mismo en un binario de ~30MB y compila mucho más rápido.
# OJO: pandoc necesita sí o sí -V mainfont=... o typst 0.15 aborta con
# "font fallback list must not be empty". Ya va en la función.
pandoc-cli
typst

# --- Yazi + previews / papelera ---
yazi
ffmpeg
7zip
ouch
jq
poppler
imagemagick
trash-cli

# --- Shell / terminal ---
starship
zoxide
fastfetch

# --- Sistema ---
sddm
timeshift
rtkit
nano
less
ntfs-3g
ntfsprogs

# --- Java + IDEs (repos oficiales) ---
jdk-openjdk
maven
netbeans

# --- Apps de escritorio ---
anki
obs-studio
mpv
imv
kate
zathura
zathura-pdf-mupdf
plasma-browser-integration
nwg-displays

# --- Wallpaper Engine (requiere repo multilib, lo habilita bootstrap.sh) ---
steam

# --- Virtualización (VMs con virt-manager) ---
qemu-full
libvirt
virt-manager
dnsmasq
edk2-ovmf

# --- Transcriptor de audio del sistema (~/.config/transcriptor) ---
# uv suele venir como dependencia de otro paquete: va acá para que quede
# explícito y pacman no se lo lleve al desinstalar algo. El resto (pipewire,
# libpulse, libnotify) ya está más arriba o lo trae el grupo illogical-impulse.
uv
