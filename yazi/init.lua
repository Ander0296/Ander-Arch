require("chronos"):setup({ enable = true, notify_mode = "summary" })
require("folder-rules"):setup()
require("starship"):setup()
require("recycle-bin"):setup({ trash_dir = "/home/ander/.local/share/Trash/" })
